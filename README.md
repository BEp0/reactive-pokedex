# reactive-pokedex

## Run the application:

`gradle build`

## Endpoints:

- `GET /pokemons`
- `GET /pokemons/events`
- `GET /pokemons/{id}`
- `POST /pokemons`

## Data Base:
MongoDB

## Postman:
The file name is `reactive-pokedex-api.postman_collection.json` and it's on folder `data`
